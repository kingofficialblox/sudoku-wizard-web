[DEPENDENCIES]
# The published Pages files live in docs/.  Excluding that directory prevents
# each new browser build from packaging the previous browser build inside it.
ignoreDirs = ["docs"]
ignoreFiles = []
